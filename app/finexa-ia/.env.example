# Copiá a `.env` en la raíz de `finexa-ia`. Tras `terraform apply` en infra/environments/mvp: `terraform output cognito_user_pool_id` y `cognito_client_id` (o consola Cognito).
EXPO_PUBLIC_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
EXPO_PUBLIC_COGNITO_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
